import Vue from 'vue';//这行必须加，不然会报错
import {
     Button, 
     Select,
     Row,
     Col,
     TimeSelect,
     DatePicker,
} from 'element-ui';

Vue.use(Button)
Vue.use(Select)
Vue.use(Row)
Vue.use(Col)
Vue.use(TimeSelect)
Vue.use(DatePicker)